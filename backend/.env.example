# Phase 2 — optional local notes (secrets go in Modal dashboard)
# MODAL_APP_NAME=karaoke
# MODAL_API_URL_PROD=https://jacoblum22--karaoke-api.modal.run
# MODAL_API_URL_DEV=https://jacoblum22--karaoke-api-dev.modal.run
# JOBS_DICT_NAME=karaoke-jobs
# STUB_VIDEO_URL=https://automatic-karaoke.vercel.app/sample.mp4  (see orchestrator.py)

# Phase 3 — local Demucs (not used by lean web image):
# pip install -r backend/requirements-demucs.txt

# Phase 4 — local Whisper (not used by lean web image):
# pip install -r backend/requirements-whisper.txt

# Phase 2+ — Modal secrets preferred over .env in production
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=
# R2_ENDPOINT_URL=
